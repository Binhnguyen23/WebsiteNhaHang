from app import app # (app là đối tượng "Flask" từ trong file "app.py")

if __name__ == '__main__': # lệnh thực thi cho phương thức "run()" của đối tượng "app"
    app.run(debug=True) # gọi phương thức "run()" từ đối tượng "app" # --> 


                                              